# 青少年心理健康促进管理平台

> 一个基于Spring Boot + Vue 2的完整心理健康管理平台，用于青少年心理健康评估、预警和管理。

## 📋 项目简介

本系统旨在通过多维度数据分析，为青少年提供心理健康评估与预警服务。系统整合临床量表、生活规律、语音特征等多模态数据，基于机器学习算法精准评估心理健康风险等级，及时发现高危倾向，辅助医生进行干预决策。

### 核心功能

- **多维评估** - 整合临床量表、生活规律、语音特征等多模态数据
- **智能预测** - 基于机器学习算法，精准评估心理健康风险等级
- **实时预警** - 及时发现高危倾向，辅助医生进行干预决策
- **数据管理** - 完整的医院、医生、患者、量表等数据管理功能
- **权限控制** - 基于角色的权限管理系统

## 🛠 技术栈

### 后端技术

- **框架**: Spring Boot 2.7.6
- **数据库**: MySQL 8.0
- **ORM**: MyBatis-Plus 3.5.7
- **API文档**: Swagger/Knife4j
- **认证**: JWT
- **构建工具**: Maven
- **Java版本**: JDK 8+

### 前端技术

- **框架**: Vue 2.6.10
- **UI组件**: Element UI 2.13.2
- **路由**: Vue Router 3.0.6
- **状态管理**: Vuex 3.1.0
- **HTTP客户端**: Axios 0.18.1
- **图表库**: ECharts 5.6.0
- **构建工具**: Vue CLI 4.4.4

## 📁 项目结构

```
depression_project/
├── backend/                    # 后端项目
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/io/depression/
│   │   │   │   ├── common/          # 通用类（Result等）
│   │   │   │   ├── controller/     # 控制器层（8个Controller）
│   │   │   │   ├── dto/            # 数据传输对象（17个DTO）
│   │   │   │   ├── entity/         # 实体类（9个Entity）
│   │   │   │   ├── mapper/         # Mapper接口（9个Mapper）
│   │   │   │   ├── service/        # 服务层
│   │   │   │   │   └── impl/       # 服务实现类（8个Service）
│   │   │   │   └── vo/             # 视图对象（11个VO）
│   │   │   └── resources/
│   │   │       ├── db/             # 数据库脚本
│   │   │       │   ├── 01_drp_system_init.sql      # 系统初始化
│   │   │       │   ├── 02_assessment_config_update.sql  # 评定配置
│   │   │       │   ├── 03_permission_init.sql      # 权限初始化
│   │   │       │   └── 04_question_bank_init.sql   # 题库初始化
│   │   │       └── application.yml  # 配置文件
│   │   └── test/
│   └── pom.xml
│
└── frontend/                    # 前端项目
    ├── src/
    │   ├── api/                  # API接口封装
    │   ├── components/           # 公共组件
    │   │   ├── Breadcrumb/       # 面包屑导航
    │   │   ├── Hamburger/        # 菜单折叠按钮
    │   │   ├── Pagination/       # 分页组件
    │   │   └── SvgIcon/          # SVG图标组件
    │   ├── layout/               # 布局组件
    │   │   ├── components/       # 布局子组件
    │   │   └── mixin/            # 布局混入
    │   ├── router/               # 路由配置
    │   ├── store/                # Vuex状态管理
    │   ├── styles/               # 样式文件
    │   ├── utils/                # 工具函数
    │   └── views/                # 页面组件
    │       ├── login/            # 登录页
    │       ├── dashboard/        # 首页
    │       ├── hospital/         # 医院管理
    │       ├── hospital-admin/   # 医院管理员管理
    │       ├── doctor/           # 医生管理
    │       ├── patient/          # 患者管理
    │       ├── warning/          # 预警管理
    │       ├── permission/       # 权限管理
    │       ├── scale/            # 量表管理
    │       ├── user/             # 用户管理
    │       └── profile/          # 个人信息
    ├── public/                   # 静态资源
    ├── package.json
    └── vue.config.js
```

## 🚀 快速开始

### 环境要求

- JDK 8+
- Maven 3.6+
- Node.js 12+
- MySQL 8.0+
- npm 或 yarn

### 1. 数据库初始化

```sql
-- 创建数据库
CREATE DATABASE drp_system DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 使用数据库
USE drp_system;

-- 执行初始化脚本（按顺序执行）
SOURCE backend/src/main/resources/db/01_drp_system_init.sql;
SOURCE backend/src/main/resources/db/02_assessment_config_update.sql;
SOURCE backend/src/main/resources/db/03_permission_init.sql;
SOURCE backend/src/main/resources/db/04_question_bank_init.sql;
```

### 2. 后端启动

```bash
# 进入后端目录
cd backend

# 修改数据库配置
# 编辑 src/main/resources/application.yml
# 修改数据库连接信息：
#   url: jdbc:mysql://localhost:3306/drp_system?...
#   username: root
#   password: 你的密码

# 编译项目
mvn clean compile

# 运行项目
mvn spring-boot:run

# 或者使用IDE运行 DepressionSystemApplication 类
```

后端服务启动后访问：
- API文档: http://localhost:8082/doc.html
- 后端服务: http://localhost:8082

### 3. 前端启动

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install
# 或使用 yarn
yarn install

# 启动开发服务器
npm run dev
# 或
yarn dev
```

前端服务启动后访问：http://localhost:9528

### 4. 默认登录账号

- **用户名**: `admin`
- **密码**: `admin123`

## 📊 功能模块

### 1. 首页 Dashboard
- 系统概览统计（医院数、用户数、预警数等）
- 患者风险等级分布（饼图）
- 近半年趋势分析（折线图）

### 2. 医院管理
- 医院信息增删改查
- 医院状态管理
- 支持按名称/编码搜索

### 3. 医院管理员管理
- 管理员账号管理
- 权限分配
- 支持按医院筛选

### 4. 医生管理
- 医生信息管理
- 支持按医院、科室筛选
- 医生权限管理

### 5. 患者管理
- 患者信息完整管理
- 患者迁移功能
- 支持多条件查询
- 患者状态跟踪

### 6. 预警管理
- 预警记录查看
- 预警处理功能
- 按预警等级筛选
- 风险评分展示

### 7. 权限管理
- 权限树结构展示
- 权限详情查看
- 支持菜单和按钮权限

### 8. 量表管理
- 量表题目管理
- 题目选项配置
- 支持批量操作
- 题目类型：单选、多选、填空、量表评分

### 9. 用户管理
- 所有用户统一管理
- 支持按角色、医院筛选
- 用户身份变更
- 权限分配

### 10. 个人信息
- 个人信息查看和编辑
- 密码修改
- 账号信息展示

## 🔌 API接口说明

### 认证相关
- `POST /api/auth/login` - 用户登录
- `POST /api/auth/password` - 修改密码

### 首页统计
- `GET /api/dashboard` - 获取首页统计数据

### 医院管理
- `GET /api/hospitals` - 分页查询医院列表
- `GET /api/hospitals/all` - 获取所有医院列表
- `GET /api/hospitals/{cohortCode}` - 获取医院详情
- `POST /api/hospitals` - 创建医院
- `PUT /api/hospitals/{cohortCode}` - 更新医院
- `DELETE /api/hospitals/{cohortCode}` - 删除医院

### 用户管理
- `GET /api/users` - 分页查询用户列表
- `GET /api/users/{id}` - 获取用户详情
- `POST /api/users` - 创建用户
- `PUT /api/users/{id}` - 更新用户
- `DELETE /api/users/{id}` - 删除用户
- `POST /api/users/permissions` - 分配权限
- `PUT /api/users/profile/{userId}` - 更新个人信息

### 患者管理
- `GET /api/patients` - 分页查询患者列表
- `GET /api/patients/{id}` - 获取患者详情
- `POST /api/patients` - 创建患者
- `PUT /api/patients/{id}` - 更新患者
- `DELETE /api/patients/{id}` - 删除患者
- `POST /api/patients/{id}/transfer` - 患者迁移

### 预警管理
- `GET /api/warnings` - 分页查询预警列表
- `GET /api/warnings/{id}` - 获取预警详情
- `POST /api/warnings/{id}/process` - 处理预警

### 权限管理
- `GET /api/permissions/tree` - 获取权限树
- `GET /api/permissions/{id}` - 获取权限详情

### 量表题目管理
- `GET /api/scale-questions` - 分页查询题目列表
- `GET /api/scale-questions/{id}` - 获取题目详情
- `POST /api/scale-questions` - 创建题目
- `PUT /api/scale-questions/{id}` - 更新题目
- `DELETE /api/scale-questions/{id}` - 删除题目

## 🗄 数据库设计

### 核心表结构

1. **cohort_centers** - 医院表
2. **cohort_authorization** - 系统用户表（管理员/医生）
3. **patients** - 患者表
4. **warning_records** - 预警记录表
5. **sys_permission** - 功能权限配置表
6. **scale_question** - 量表题目表
7. **scale_question_option** - 题目选项表
8. **assessment_config** - 评定项目配置表
9. **patient_assessments** - 患者评定记录表

详细表结构请查看 `backend/src/main/resources/db/01_drp_system_init.sql`

## 🏗 构建部署

### 后端构建

```bash
cd backend
mvn clean package
# 生成的jar包在 target/ 目录下
java -jar target/depression-system-0.0.1-SNAPSHOT.jar
```

### 前端构建

```bash
cd frontend
npm run build:prod
# 构建产物在 dist/ 目录下
# 将 dist/ 目录部署到 Nginx 或其他 Web 服务器
```

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态资源
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # 后端API代理
    location /api {
        proxy_pass http://localhost:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

## 🔧 开发说明

### 后端开发

1. **添加新功能模块**：
   - 创建 Entity 实体类
   - 创建 Mapper 接口
   - 创建 Service 接口和实现
   - 创建 Controller 控制器
   - 创建 DTO 和 VO 类

2. **数据库操作**：
   - 使用 MyBatis-Plus 进行 CRUD 操作
   - 复杂查询可编写 XML 映射文件

3. **统一响应格式**：
   ```java
   return Result.success(data);
   return Result.error("错误信息");
   ```

### 前端开发

1. **添加新页面**：
   - 在 `src/views/` 下创建页面组件
   - 在 `src/router/index.js` 中添加路由
   - 在 `src/api/index.js` 中添加 API 接口

2. **使用 Element UI 组件**：
   ```vue
   <el-button type="primary">按钮</el-button>
   <el-table :data="tableData">...</el-table>
   ```

3. **调用后端 API**：
   ```javascript
   import { getUsers } from '@/api'
   const response = await getUsers(params)
   ```

## 📝 注意事项

1. **密码加密**: 系统使用 BCrypt 进行密码加密
2. **JWT Token**: 登录后返回 Token，需要在请求头中携带
3. **权限控制**: 基于角色的权限管理，支持菜单和按钮级权限
4. **数据校验**: 使用 JSR-303 进行数据校验
5. **逻辑删除**: 删除操作使用逻辑删除，不会真正删除数据

## 🐛 常见问题

### 1. 后端启动失败
- 检查数据库连接配置是否正确
- 确认数据库已创建并执行了初始化脚本
- 检查端口 8082 是否被占用

### 2. 前端无法连接后端
- 检查 `vue.config.js` 中的代理配置
- 确认后端服务已启动
- 检查跨域配置

### 3. 登录失败
- 确认数据库中有默认用户数据
- 检查用户名和密码是否正确
- 查看后端日志错误信息

## 📈 开发计划

- [x] 基础功能模块开发
- [x] 前后端联调
- [ ] 完善权限控制机制
- [ ] 添加操作日志记录
- [ ] 实现数据导出功能
- [ ] 添加消息通知功能
- [ ] 完善异常处理机制
- [ ] 性能优化

## 📄 许可证

本项目仅供学习和研究使用。

## 👥 贡献

欢迎提交 Issue 和 Pull Request。

## 📮 联系方式

如有问题，请联系开发团队。

---**最后更新**: 2025年1月
